Extraction phase — CIV pilot, v0.2 validation run. Governing docs are fixed in the project files: coverage map, processing plan, decision rules (incl. §7 assignment_type criterion, §8 program precedent, §9 lineage-deferral rule, §10 reconciliation-deferred), assumptions register, evidence dictionary v0.2, gold standard v2. Read and apply them; use v0.2, not v0.1. Do not reopen settled decisions.
Operative test (binding, apply even if retrieval is imperfect): an earmark requires BOTH (a) a source levy/tax/redevance/contribution/prélèvement AND (b) a verbatim clause assigning its proceeds — in whole or in part — to a specific destination. Purpose-restriction of use, not recipient identity, is the scope gate (is_purpose_restricted): a tax → a purpose-restricted fund/organism/programme is in scope; a tax → a tier's general/unrestricted budget is recorded but out of scope. A fund merely existing is not sufficient. Where the ENACTING text names no destination, destination stays null / confidence medium — the exposé des motifs is motivation, not assignment; never infer an unnamed fund. Functional-purpose destinations (a named activity) count as genuine assignments (beneficiary_type = program).
Task, this session only: populate the evidence table (v0.2) end-to-end for the attached 2024 annexe fiscale. Id-free — record natural keys (document_id, legal_article, official_name verbatim, verbatim_excerpt), leave instrument_id/pair_id blank; every row carries verbatim excerpt + page. Fund-agnostic three-layer retrieval (assignment-grammar sweep + heading pass + recall audit), never a known-fund list.
Apply the v0.2 mechanics: banded rates/shares → scalar field null, *_is_schedule = 1, schedule in *_schedule_detail (never text in a numeric column); set share_basis ∈ {stated, whole_proceeds_implied, deferred_arrete}; set is_purpose_restricted and assignment_type per §7; set partial_key = 1 when a clause restates only amended tiers. Lineage (§9): populate predecessor_ref only when the clause itself cites a prior instrument/article, and predecessor_relation ∈ {amends, replaces, merges, splits, none} for what the text states the relationship is — a recorded signal, not a judgment. Set change_type/structural_break provisionally, human_validation_status = unchecked. Make NO cross-document identity or lineage judgment — that is deferred to reconciliation.
Deliverables: (1) the populated evidence rows; (2) a v0.2 field-exercise report: which new v0.2 fields fired (rate/share schedule triple, share_basis, is_purpose_restricted/assignment_type, partial_key, predecessor_relation, source_internal_inconsistency) and which never triggered — and whether a non-firing field is a document property or a schema smell; (3) a schema-stress list: any clause that still didn't fit, forced an unanticipated null, or needed a judgment the rules don't cover; (4) per non-high-confidence row, the reason (schema problem vs document problem); (5) score against the AF2024 gold case GOLD_03 (CIAPOL / Fonds National de l'Environnement, 30/50/20): recall + any invented field.
Deferral instruction (do NOT resolve): the AF2024 CIAPOL/FNE case is the reconciliation counterpart to the AF2025 chemical-pollution tax (GOLD_04). Extract the AF2024 clause on its own terms; if it contains lineage language, record it in predecessor_ref/predecessor_relation, otherwise leave null. Do NOT attempt to link it to AF2025 — you cannot see that document, and the cross-document judgment belongs to reconciliation. Simply note in notes that reconciliation must examine the GOLD_03↔GOLD_04 relationship.