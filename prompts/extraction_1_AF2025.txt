Extraction phase — CIV pilot, evidence-table population. Governing decisions are fixed in the project files: coverage map, processing plan, decision rules, assumptions register, evidence dictionary v0.1, gold standard v2. Read and apply them; do not reopen them.
Operative definition (tripwire — apply even if retrieval is imperfect): An earmarked tax dedicates revenue from a specific levy to a designated destination rather than the general budget. Binding operative test: an earmark requires BOTH (a) a source levy/tax/redevance/contribution/prélèvement AND (b) a verbatim clause assigning its proceeds — in whole or in part — to a specific destination (fund, agency, ministry, local government, sector, programme, or supranational body). A fund merely existing is not sufficient. Where the source names no destination, destination stays null and confidence is medium — never infer an unnamed fund. Para-fiscal and partial-share assignments are in scope.
Task for this session only: populate the evidence table (v0.1 schema) end-to-end for the attached 2025 annexe fiscale. Extraction is id-free — record the natural keys (document_id, legal_article, official_name verbatim, verbatim_excerpt), leave instrument_id/pair_id blank. Every row carries a verbatim excerpt and page. Use the fund-agnostic three-layer retrieval (assignment-grammar sweep + heading pass), never a known-fund list.
Deliverables: (1) the populated evidence rows; (2) a schema-stress list — every clause that didn't fit a column, forced an unanticipated null, or required a judgment the decision rules don't cover; (3) for every non-high-confidence row, state the specific reason (e.g. "destination unnamed in source" vs "OCR ambiguous on %") so I can tell schema problems from document problems. Then score against AF2025 gold cases (02, 04, 06): report recall and flag any invented field. Watch mechanism (may be instrument-level not clause-level) and the change_type/structural_break trio (set provisional, mark unchecked — finalized in reconciliation).
